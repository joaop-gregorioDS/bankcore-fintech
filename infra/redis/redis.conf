# Redis is reachable only through the private Docker network; the Compose file
# intentionally publishes no Redis port. Production ACL/TLS remains an
# environment-specific concern and is not encoded in this repository config.
bind 0.0.0.0
protected-mode no
appendonly yes
appendfsync everysec
maxmemory 64mb
maxmemory-policy noeviction
