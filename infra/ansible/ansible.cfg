[defaults]
inventory = inventory/disposable.yml
roles_path = roles
interpreter_python = auto_silent
retry_files_enabled = false
stdout_callback = default
bin_ansible_callbacks = true

[privilege_escalation]
become = false
