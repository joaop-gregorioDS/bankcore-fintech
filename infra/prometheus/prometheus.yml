global:
  scrape_interval: 1s
  evaluation_interval: 1s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: bankcore-otel-collector
    static_configs:
      - targets: [otel-collector:8889]
