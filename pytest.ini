[pytest]
testpaths = tests
addopts = --strict-markers -ra
markers =
    unit: testes sem infraestrutura externa
    security: autenticação, JWT e controles de segurança
    integration: testes de integração
    postgres: testes que exigem PostgreSQL real
